# Site settings
title: APES
email: info@biom.uni-freiburg.de
description: Advice for Problems in Environmental Statistics
baseurl: /APES
url: http://biometry.github.io
username: biometry

# Build settings
markdown: redcarpet
permalink: pretty

# Excludes
exclude: ['APES.sublime-project', 'Gemfile', 'Gemfile.lock', 'feed.xml']

# github gems
# gems:
#   - jekyll-mentions

# Default page settings, currently not working on gh-pages
defaults:
  -
    scope:
      path:
      type: page
    values:
      author: Florian Hartig
      layout: page
  -
