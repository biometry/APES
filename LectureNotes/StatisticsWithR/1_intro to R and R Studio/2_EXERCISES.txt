1) in microsoft excel, create a fake dataset with 15 rows and 3 columns (Assign the headers id, colA,gender).
Include few decimal numbers (depending on your computer configuration, decimals will be indicated by commas "," or by periods ".")
Use NA for few cells 
Use charater for one column (e.g., male, female,NA)


2) save 2 copies of the file 1) as a comma delimited .csv
   			     2) as a tab delimited .txt

3) import them in R, run basic tools for data exploration 
(summary, head, tail) and verify that data have been 
imported properly as numbers. 